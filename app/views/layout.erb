<!doctype html>
<html>
  <head>
    <title>Football Roster Builder</title>
  </head>
  <body>
      <nav>
        <% if logged_in? %>
          <a href="/players/new"> Add Player </a>
          <a href="/logout"> Log Out </a>
          <p>Welcome, <%=current_user.username %></p>
        <% else %>
          <a href="/signup">Sign Up</a>&nbsp
          <a href="/login"> Log In </a>
        <%end %>
     </nav>

      <%= yield %>

  </body>
</html>